locals {
  name_prefix = "${var.project}-${var.environment}"
}

# Milestone 7 will expand this module into ECS/Fargate, RDS Postgres with PostGIS,
# ElastiCache Redis, S3 raw snapshots, OpenSearch, and CloudWatch dashboards.

